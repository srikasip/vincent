= form_for @vaccine do |f|
  - if @vaccine.errors.any?
    #error_explanation
      %h2= "#{pluralize(@vaccine.errors.count, "error")} prohibited this vaccine from being saved:"
      %ul
        - @vaccine.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :due_range
    = f.number_field :due_range
  .actions
    = f.submit 'Save'
